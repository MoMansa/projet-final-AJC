apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: foodtrack-test

resources:
  - namespace.yaml
  - ingress.yaml
  - ../../base

replicas:
  - name: portail-qualite
    count: 2
  - name: api-capteurs
    count: 2

patches:
  - path: patch-configmap.yaml
    target:
      kind: ConfigMap
      name: foodtrack-config

secretGenerator:
  - name: foodtrack-api-config
    envs:
      - secret.env
